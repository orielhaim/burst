# API base URL used by the Better Auth React client and future HTTP calls
VITE_API_URL=http://localhost:3001

# Colyseus game server WebSocket URL (used when the game client is wired up)
VITE_GAME_SERVER_URL=ws://localhost:2567
